# Flibusta FB2 extraction configuration.
#
# These values are intentionally local-environment settings. Keep real local
# paths out of source-controlled scripts whenever possible.
#
# Precedence for this first version:
#   environment variable > value defined here
# -----------------------------------------------------------------------------

: "${FLIBUSTA_SOURCE_DIR:=/mnt/x/flibusta}"
: "${FB2_OUTPUT_DIR:=/mnt/c/Backup_Go7/ToLoad}"

export FLIBUSTA_SOURCE_DIR
export FB2_OUTPUT_DIR
