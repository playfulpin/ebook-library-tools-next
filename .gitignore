# Scratch and generated files — keeps `git status` clean during development.

# Agent/editor working directories
.freebuff/

# Archived/scratch directories
_Old_Stuff/
_Save_Stuff/
commit_msg/
reports/

# Raw flibusta dump archives (lib*.sql.gz and decompressed lib*.sql) —
# never committed, they live on disk only
data/archives/flibusta_gz/
data/archives/flibusta/

# Temporary outputs and captures
tmp_*
SHELL_tree
jnk*
j_Copilot
j_original
out_*.txt
*utf8*
!test_utf8_prefix_generator.sh
!lib/utf8_prefix_generator.awk

# Compiled artifacts (sources prefix_children*.c are tracked)
prefix_children

# Editor backups
*.bak

# Merge-tool generated reports (written to $PWD when --report-dir is unset)
merge_books_*.tsv
merge_skeleton_into_books_*.tsv
